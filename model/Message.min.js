'use strict';const mongoose=require("mongoose"),Schema=mongoose.Schema,newMessage=new Schema({name:{type:String},message:{type:String},date:{type:Date,default:new Date}});module.exports=mongoose.model("chats",newMessage);